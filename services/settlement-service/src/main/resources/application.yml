server:
  port: 8090

spring:
  application:
    name: settlement-service

  datasource:
    url: jdbc:postgresql://localhost:5432/paycore_settlement
    username: postgres
    password: password
    driver-class-name: org.postgresql.Driver

  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update
    show-sql: false
