server:
  port: 8088

spring:
  application:
    name: capture-service

  datasource:
    url: jdbc:postgresql://localhost:5432/paycore_capture
    username: postgres
    password: password
    driver-class-name: org.postgresql.Driver

  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update
    show-sql: false
